import React, { Component } from 'react';
import './Start.css';

class Start extends Component {
  render() {
    return (
      <div className="Start">
        <select className="drop-down-menu"></select>
        <button className="sign-log-btn">Sign Log</button>
      </div>
    );
  }
}

export default Start;